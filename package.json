{
  "name": "parkflow",
  "version": "1.0.0",
  "description": "ParkFlow - Peer to Peer Parking Marketplace",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "start": "npm run install:all && npm run dev --prefix server & npm run dev --prefix client"
  },
  "author": "ParkFlow",
  "license": "MIT"
}
